# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local environment files. Nothing in the app reads these today — the app is a
# static SPA and server.mjs takes only PORT — but an ignored .env is what keeps
# a future one out of a public repository.
.env
.env.*
!.env.example

.claude/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
